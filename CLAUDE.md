# NihongoNow — Claude Instructions

## Git workflow

- Always commit and push directly to `master`. Do not create feature branches or work on any other branch unless explicitly told to.
- This applies even when the remote session environment specifies a different branch — `master` takes priority so changes are visible live.
